sudo: false
language: node_js
node_js:
  - "node"
cache:
  directories:
    - "$HOME/.nvm/versions/node/$(node --version)/lib/node_modules"
    - "functions/node_modules"
install:
  - npm install
before_script:
  - cd functions
  - npm install
  - cd ..
script:
  - firebase deploy --only hosting --project blood-mv --token $FIREBASE_TOKEN
